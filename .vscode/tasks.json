{
  "version": "2.0.0",
  "tasks": [
    {
      "detail": "Build the project using CMake",
      "label": "CMake Build",
      "type": "shell",
      "command": "cmake --build build -- -j$(nproc)",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}
